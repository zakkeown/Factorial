[package]
name = "factorial-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
factorial-core = { path = "../factorial-core" }

[build-dependencies]
cbindgen = "0.27"
